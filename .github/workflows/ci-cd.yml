name: CI Beta to Main

on:
  push:
    branches: [ beta ]

jobs:
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Debug File Structure
        run: ls -R
        
      - name: Run Pytest
        run: |
          export PYTHONPATH=$PYTHONPATH:.
          python -m pytest -v

  merge-to-main:
    name: Auto-Merge to Main
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Merge Beta to Main
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Fetch all branches to ensure we can switch
          git fetch origin main:main
          git checkout main
          
          # Merge beta into main (using target commit directly)
          git merge origin/beta --no-ff -m "chore: auto-merge beta to main [skip ci]"
          
          # Push to production branch
          git push origin main
